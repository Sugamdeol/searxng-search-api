# SearXNG Search API Configuration

# Hostname for SearXNG (used in production)
SEARXNG_HOSTNAME=search.yourdomain.com

# Secret key for SearXNG (generate with: openssl rand -hex 32)
SEARXNG_SECRET=your-secret-key-here

# Cache TTL in seconds (default: 1 hour)
CACHE_TTL=3600

# Max results per query (default: 100)
MAX_RESULTS=100

# For local development, these are set in docker-compose.yml
# SEARXNG_URL=http://searxng:8080
# REDIS_URL=redis://redis:6379
